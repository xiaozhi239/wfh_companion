name: wfh_companion
description: Work from home companion 

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  noise_meter: ^1.0.1
  flutter_duration_picker: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  assets:
    - assets/images/

  uses-material-design: true
